# ==========================================
# ⚙️ MYQUEST BACKEND - VARIABLES D'ENVIRONNEMENT
# ==========================================
#
# Ce fichier liste toutes les variables nécessaires.
# Copie-le en .env et remplace les valeurs:
#   cp .env.example .env
#
# ⚠️ IMPORTANT: Ne commit JAMAIS le fichier .env!
# Il est dans .gitignore pour cette raison.

# ============================================
# 🗄️ BASE DE DONNÉES
# ============================================
# URL de connexion PostgreSQL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
#
# LOCAL (avec Docker):
#   DATABASE_URL=postgresql://postgres:postgres@localhost:5432/myquest
#
# RENDER (automatique):
#   Render injecte cette variable automatiquement
#   Tu n'as rien à configurer!

DATABASE_URL=postgresql://user:password@localhost:5432/myquest

# ============================================
# 🔐 AUTHENTIFICATION JWT
# ============================================
# Clé secrète pour signer les tokens JWT
# GÉNÈRE UNE VRAIE CLÉ en production!
# Commande: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# RENDER: généré automatiquement via generateValue: true

JWT_SECRET=change-this-to-a-real-secret-key-in-production

# Durée de validité du token (optionnel)
# Défaut: 7d (7 jours)
# JWT_EXPIRES_IN=7d

# ============================================
# 🌐 SERVEUR
# ============================================
# Port d'écoute du serveur
# RENDER: injecte automatiquement le port
# LOCAL: 3000 par défaut

PORT=3000

# Environnement d'exécution
# - development: erreurs détaillées, logs verbeux
# - production: erreurs génériques, optimisations

NODE_ENV=development

# ============================================
# 📝 VARIABLES OPTIONNELLES (FUTURES)
# ============================================
# Ces variables seront utiles plus tard

# URL du frontend (pour CORS)
# FRONTEND_URL=http://localhost:8081

# Clé API pour les notifications push
# EXPO_PUSH_TOKEN=your-expo-push-token

# Clé API pour l'IA (coach IA)
# OPENAI_API_KEY=sk-xxx

# Email (pour password reset)
# SMTP_HOST=smtp.gmail.com
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
